import React from "react";

import "./Map.scss"

export default function Map({coordinates, lat, long, country, capital, iso}) {
    return (
        <section className="map">

        </section>
    )
}